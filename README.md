# jupyter-workbench

Reusable Jupyter session, notebook lineage, and visualization skill package.

For headless local rendering, prefer `trame-vtklocal` via `launch_vtklocal_view()` over PyVista's vtk.js-backed `plotter.show(jupyter_backend="trame")` client mode.
